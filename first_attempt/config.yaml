dataset_root: C:\Users\depthlev\Desktop\droneImages\dataset
strict: true
max_samples: null

depth_model:
  model_name: depth-anything/Depth-Anything-V2-Small-hf
  device: null

split:
  seed: 20260426
  holdout_fraction: 0.15
  num_folds: 5

output_root: first_attempt/artifacts
